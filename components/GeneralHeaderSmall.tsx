const GeneralHeaderSmall = ({headerText}: {headerText: string}) => {
    return (
        <h1 className="text-lg text-teal-700">
            {headerText}
        </h1>
    )
}

export default GeneralHeaderSmall